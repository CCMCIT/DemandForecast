[pytest]
pythonpath = src
testpaths = tests
addopts = --strict-markers
markers =
    unit: offline tests with no external dependencies
    integration: tests that hit the live database (require a working .env connection)
